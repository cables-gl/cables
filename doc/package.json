{
  "name": "gitbook_ops_add",
  "version": "0.1.0",
  "description": "You need to add a symlink called \"ops_base\" pointing to \"src/ops/base\" first. Will add all ops with a markdown file to the doc index (summary).",
  "main": "add_ops.js",
  "scripts": {
    "api": "jsdoc2md --configure jsdoc.json --partial jsdoc/header.hbs --files ../src/core/*.js > content/core_api/CORE_API.md",
    "prepare": "gitbook install",
    "build": "npm run prepare && npm run api && gitbook build",
    "serve": "npm run prepare && npm run api && gitbook serve --no-live",
    "clean": "rimraf _book"
  },
  "author": "Tim Pulver",
  "license": "ISC",
  "engines": {
    "node": ">=8"
  },
  "dependencies": {
    "gitbook-plugin-ga": "^1.0.1"
  },
  "devDependencies": {
    "@babel/core": "^7.2.2",
    "fs-sync": "^1.0.6",
    "gitbook-plugin-ga": "^2.0.0",
    "jsdoc-babel": "^0.5.0",
    "jsdoc-to-markdown": "^4.0.1",
    "rimraf": "^2.6.3"
  }
}
